""" 
strings :
    aspas triplas : permite quebra de linha
    concatena com +
    compara com o == (case sensitive)    
    .lower() converte pra lower
    .upper() converte pra upper
    .capitalize() a primeiro pra maiscula
    .title() inicial de cada palavra
    x in string -> pesquisa string
    .find() retorna o index de certa substring e -1 se n encontrar
    .count(substring,inicio,fim) conta a quantidade de certa substring
    join() pra converter uma lista de strings ou chars em uma string
    split("separador") divide a string em lista de substrings a partir de um separador
    strings são imutaveis
"""